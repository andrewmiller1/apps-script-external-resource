// Use this in another project that uses the API library
/*
function testingatarstartnioarsntoistitnstasrtrno() {
  ExternalResource.add("objectis", "https://gist.githubusercontent.com/matthewp/2036428/raw/acad17ba614efd2e6192699719fd10ab90c3cbe9/gistfile1.js");
  //ExternalResource.add("poly", "https://raw.githubusercontent.com/inexorabletash/polyfill/master/polyfill.js")
  Logger.log(ExternalResource.resources);
  //ExternalResource.load(this);
  //Logger.log(es5);
  //var variable = ExternalResource.run(ExternalResource.resources.es5);
  //Logger.log(variable);
  //ExternalResource.run(ExternalResource.resources.objectis);
  ExternalResource.load();
  var runtypetrasntia = ExternalResource.run.RunType.Namespace;
  Logger.log(runtypetrasntia);
  var vari = ExternalResource.run;
  Logger.log(vari);
  var object1 = { myProp: "hello there" };
  var object2 = object1;
  var object3 = { myProp: "hello there" };
  var object4 = { hi: "hey" };
  
  var vari1 = Object.is(object1, object2);
  var vari2 = Object.is(object1, object3);
  var vari3 = Object.is(object1, object4);
  Logger.log(vari1);
  Logger.log(vari2);
  Logger.log(vari3);
}
*/
